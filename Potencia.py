'''Codigo para hacer una potenciación, primero definimos variables, luego el usuario introduce las variables a traves del teclado'''
base= float(input("ingrese la base: "))
exponente = float(input("ingrese el exponente:"));  
potencia = base**exponente;
#Resultao:
print("el numero:  " +  str(base) + "elevado a: "  +  str(exponente) + "es igual a: " + str(potencia))
 

